{
  "name": "@glitch/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared pure, deterministic gameplay logic (future home of GameCore). No DOM, network, audio, or React imports.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  }
}
